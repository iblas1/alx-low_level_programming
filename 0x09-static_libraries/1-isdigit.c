#include "main.h"

/**
* _isdigit - returns the lower case
* @c: character to check
* Return: lower case character
*/

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
	return (0);
	}
}
