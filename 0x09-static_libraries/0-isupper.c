#include "main.h"

/**
* _isupper - returns the lower case
* @c: character to check
* Return: lower case character
*/

int _isupper(int c)
{

	if (c >= 'A' && c <= 'Z')
	return (1);

	else
	return (0);
}


